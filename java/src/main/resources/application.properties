spring.application.name=demo

# 🚀 FIXED: Tell Spring to look at the environment variables passed by AWS
# 1. Change the driver class to the AWS Wrapper
spring.datasource.driver-class-name=software.amazon.jdbc.Driver

# 2. Add "jdbc:aws-wrapper:postgresql:" to the URL string and activate iam
spring.datasource.url=jdbc:aws-wrapper:postgresql://://amazonaws.com

# 3. Strip out any static password properties
spring.datasource.username=postgres


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=6379
spring.data.redis.lettuce.pool.max-active=50

# Lettuce Driver Connection Pool Adjustments for High Concurrent Load
spring.data.redis.lettuce.pool.max-active=64
spring.data.redis.lettuce.pool.max-idle=16
spring.data.redis.lettuce.pool.min-idle=4
spring.data.redis.lettuce.command-timeout=1500ms

# Force the use of SSL/TLS to match AWS forced In-Transit Encryption
spring.data.redis.ssl.enabled=true



