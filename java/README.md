 Scoring Logic
 
 The system ranks properties dynamically out of 100 total points using a split algorithm tracking two specific customer desires: 
 Target Budget matching and Market Recency velocity [3.4].
 
 Budget Variance (Max 70 Points): Properties priced equal to or lower than the user's targetBudget receive a complete 70-point credit. 
 Properties over budget degrade linearly according to how far they exceed the price threshold:\(\text{Score}=\max \left(0,\;70.0\times \left(1.0-\frac{\text{Price}-\text{Budget}}{\text{Budget}}\right)\right)\)Timeline Aging Decay (Max 30 Points): Active listings less than 7 days old get full credit. Beyond one week, scores decay at a 10% weekly compound degradation rate to prioritize fresh data:\(\text{Score}=\max \left(0,\;30.0\times 0.90^{\text{weeks}}\right)\)Tie-Breaker Rule: If scores tie exactly, the algorithm inspects the database raw listedDate attribute to position the newest inventory first [3.4].⚖️ Architectural Trade-off DecisionsIn-Memory Streaming Pagination vs. Native Index Calculations: Custom ranking algorithms require calculating dynamically changing parameters that can't easily be cached in standard SQL indexes. To optimize logic execution, the service collects filtered dataset listings into an in-memory Java Stream to execute calculations and custom sorting before slicing the array for pagination bounds [3.4]. For enterprise workloads with millions of rows, this calculation sequence should be rewritten as a custom PostgreSQL extension function.Loose Description Lookups: String wildcard filters use a case-insensitive LIKE LOWER(CAST(... AS string)) pattern [3.3]. It works perfectly natively, but it does not support advanced full-text indexing, fuzzy search parameters, or synonym matching.Your entire architecture stack is now fully wired, documented, and verified. Would you like assistance writing a database script to batch load a custom test dataset next?# property-management
